"""
Public package exports for `prompits.practices`.

Prompits provides the core HTTP-native agent runtime, Plaza coordination layer, and
pool/practice infrastructure for FinMAS. Within Prompits, the practices package bundles
reusable behaviors that agents can mount or execute remotely.

It primarily establishes package boundaries and any convenience imports expected by
neighboring modules.
"""
